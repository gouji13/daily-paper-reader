---
title: "ASymPO: Asymmetric-Scale Policy Optimization for Asynchronous LLM Post-Training Without Behavior Information"
title_zh: "ASymPO: 用于异步LLM后训练的无行为信息非对称尺度策略优化"
authors: "Zehua Liu, Yuxuan Yao, Xiaojin Fu, Tao Zhong, Mingxuan Yuan"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03070v2"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 提出非对称尺度策略优化用于异步LLM后训练
tldr: 针对异步语言模型后训练中策略梯度因陈旧响应导致分布漂移的问题，常规方法依赖行为策略概率等稳定训练。本文提出不对称尺度策略优化（ASymPO），仅使用当前策略概率，通过对各响应token损失按其平均负对数概率归一化，消除正负损失尺度失衡，无需行为信息。在异步数学推理后训练中，ASymPO有效稳定训练并保持有效学习信号。
source: arxiv
selection_source: fresh_fetch
motivation: 异步RL中陈旧响应导致分布漂移，现有行为修正方法需要行为概率，增加了系统复杂性。
method: 提出ASymPO，将每个响应的token损失除以其当前平均token负对数概率，恢复零和平衡，无需行为概率。
result: 在异步数学推理后训练中，ASymPO和SPO作为仅使用当前策略概率的目标，能够稳定训练。
conclusion: ASymPO无需行为概率即可解决异步后训练中的尺度失衡问题，简化了系统。
---

## 摘要
异步强化学习可以通过将响应生成与策略优化解耦来提高语言模型后训练的吞吐量，但过时的响应会引入分布漂移。标准的带行为校正的方法通过行为策略概率、重要性比率或剪裁来控制这种漂移，这要求在生成和学习系统之间具有令牌对齐、版本化且数值一致的行为对数概率。我们探究是否可以使用仅当前策略概率来稳定异步组相对强化学习。我们识别出一种尺度不平衡失效模式：当在当前策略下评估过时的响应时，正负损失项可能出现在不同的负对数概率尺度上，因此零和优势不再意味着平衡的损失贡献。我们提出非对称尺度策略优化（ASymPO），该方法通过每个响应的当前平均令牌负对数概率来归一化其令牌损失。ASymPO不需要行为策略概率，恢复了响应级别的零和平衡，并保留了非零学习信号。我们还引入了定标策略优化（SPO），这是一个固定的负缩放基线，并在异步数学推理后训练中评估了这两种仅使用当前策略的目标。

## Abstract
Asynchronous reinforcement learning can improve language-model post-training throughput by decoupling response generation from policy optimization, but stale responses introduce distribution drift. Standard behavior-corrected methods control this drift with behavior-policy probabilities, importance ratios, or clipping, which requires token-aligned, versioned, and numerically consistent behavior log-probabilities across rollout and learner systems. We ask whether asynchronous group-relative RL can instead be stabilized using only current-policy probabilities. We identify a scale-imbalance failure mode: when stale responses are evaluated under the current policy, positive and negative loss terms can appear at different negative log-probability scales, so zero-sum advantages no longer imply balanced loss contributions. We propose Asymmetric-Scale Policy Optimization (ASymPO), which normalizes each response's token loss by its current average token negative log-probability. ASymPO requires no behavior-policy probabilities, restores response-level zero-sum balance, and preserves a nonzero learning signal. We also introduce Scaled Policy Optimization (SPO), a fixed negative-scaling baseline, and evaluate both current-policy-only objectives in asynchronous mathematical reasoning post-training.

---

## 论文详细总结（自动生成）

# 论文 ASymPO 中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：在大型语言模型（LLM）的后训练阶段，异步强化学习通过将响应生成与策略优化解耦，可显著提升吞吐量。然而，过时的（stale）响应会导致当前策略与生成时的行为策略之间存在**分布漂移**，使得标准策略梯度方法失效。
- **现有问题**：传统方法依赖行为策略概率（behavior-policy log-probabilities）进行校正，例如使用重要性比率或剪裁机制。这要求生成系统和学习系统之间保持令牌对齐、版本一致且数值稳定的行为对数概率，增加了系统复杂性和部署成本。
- **核心问题**：是否可以在**仅使用当前策略概率**的条件下，稳定异步组相对强化学习（Group-relative RL）的训练？进一步地，为何仅使用当前策略概率会失效，以及如何修正。

## 2. 论文提出的方法论：核心思想与关键技术

- **核心思想**：识别出仅使用当前策略概率时的**尺度不平衡失效模式**：当在当前策略下评估过时的响应时，正负损失项可能分布在不同的负对数概率尺度上，导致零和优势（zero-sum advantages）不再对应平衡的损失贡献。为此，提出**非对称尺度策略优化（ASymPO）**。
- **关键细节**：
  - ASymPO 对每个响应（response）的 token 损失，除以其在当前策略下的**平均 token 负对数概率**，从而恢复响应级别的零和平衡。
  - 该方法完全不需要行为策略概率，简化了系统结构。
  - 同时引入一个基线方法**定标策略优化（SPO）**，采用固定的负缩放参数。
- **算法流程（文字描述）**：
  1. 异步收集过时响应（stale responses）及其优势值。
  2. 在当前策略下计算每个响应的平均 token 负对数概率。
  3. 对每个 token 的损失（如策略梯度损失）进行归一化：损失除以该响应的平均 token 负对数概率。
  4. 使用归一化后的损失进行策略更新，使正负损失项在尺度上恢复对称，保留有效的学习信号。

## 3. 实验设计

- **数据集/场景**：论文在**异步数学推理后训练**（asynchronous mathematical reasoning post-training）场景中进行评估。具体数据集名称未在摘要中明确给出（可能为数学推理类数据集，如 GSM8K、MATH 等，但原文未提及）。
- **基准（benchmark）**：对比了标准异步组相对策略优化方法（需行为概率）以及提出的 SPO 和 ASymPO（仅当前策略概率）。
- **对比方法**：
  - 行为的校正方法（如带重要性比率或剪裁的 PPO 变体）
  - 仅当前策略概率的基线：SPO（固定缩放）
  - 仅当前策略概率的提出方法：ASymPO

## 4. 资源与算力

- **未明确说明**：论文摘要中未提及使用的 GPU 型号、数量、训练时长等资源信息。如需完整算力细节，需查阅全文。

## 5. 实验数量与充分性

- **实验数量**：摘要仅提及在异步数学推理后训练中评估了 ASymPO 和 SPO 两种仅使用当前策略的目标。未说明具体实验组数（如不同超参数、不同数据集大小、消融实验等）。
- **充分性判断**：由于缺乏全文细节，无法全面判断实验的充分性和公平性。从摘要看，论文识别了关键失效模式并提出了解决方案，但缺少对多场景（如对话、代码生成）和不同异步程度的系统性验证。可能存在**实验覆盖不广**的风险。

## 6. 论文的主要结论与发现

- ASymPO 和 SPO 作为仅使用当前策略概率的目标，能够**稳定异步后训练**，无需行为概率。
- ASymPO 通过响应级别的零和损失归一化，**消除了正负损失尺度失衡**，同时保留了非零学习信号。
- 异步数学推理后训练中，ASymPO 表现有效，且降低了系统复杂性。

## 7. 优点：方法与实验设计的亮点

- **创新性**：首次系统性分析异步 RL 中仅使用当前策略概率时的尺度不平衡问题，并提出简洁的归一化解决方案。
- **简约性**：完全消除对行为策略概率的依赖，简化了 rollout 与 learner 系统之间的通信与版本管理。
- **普适性**：提出的归一化策略（按平均负对数概率缩放）具有通用性，可适用于其他基于优势的组相对 RL 方法。
- **理论清晰**：从零和优势与损失贡献的平衡角度解释失效原因，并给出可操作的修正。

## 8. 不足与局限

- **实验覆盖有限**：仅验证了数学推理场景，未在对话、代码生成、指令跟随等常见后训练任务中评估。
- **缺乏行为校正方法的直接对比**：摘要未给出与标准行为校正方法（如 PPO-clip）在相同条件下的性能数值比较。
- **未讨论异步程度的影响**：不同延迟程度下的鲁棒性未知。
- **资源消耗与收敛速度**：未对比训练速度、GPU 内存占用等实际部署关注点。
- **潜在偏差**：可能依赖特定的优势计算方式或奖励模型设计，未讨论泛化边界。

（完）
