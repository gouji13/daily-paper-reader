---
title: Reinforcement Learning without Ground-Truth Solutions can Improve LLMs
title_zh: 无需真实解法的强化学习可提升大型语言模型
authors: "Yingyu Lin, Qiyue Gao, Nikki Lijing Kuang, Xunpeng Huang, Kun Zhou, Tongtong Liang, Zhewei Yao, Yi-An Ma, Yuxiong He"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27369v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 无真实答案的强化学习提升LLM
tldr: "现有强化学习训练LLM通常依赖真实答案分配奖励，限制了无真实解场景的应用。本文提出RiVER框架，利用确定性执行反馈作为连续奖励，通过校准奖励塑造解决规模主导和频率主导问题。在AtCoder Heuristic Contest任务上训练后，模型在ALE-Bench排名提升8.9%和9.4%，且零样本迁移至LiveCodeBench和USACO上提升2.4%和3.5%。这表明无真实解的分数优化任务经适当奖励校准后能有效提升通用编码能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1694, \"height\": 841, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 608, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1687, \"height\": 957, \"label\": \"Table\"}]"
motivation: 传统RLVR依赖真实解，限制了对无需真实解的分数优化任务的适用性。
method: RiVER通过实例间比较和强调顶级解而保留其他有效解反馈的校准奖励塑造，处理规模主导和频率主导。
result: "训练后模型在ALE-Bench排名提升8.9%/9.4%，在LiveCodeBench和USACO上分别提升2.4%和3.5%。"
conclusion: 无真实解的分数优化任务结合适当奖励校准，可作为提升通用编码能力的有效训练环境。
---

## 摘要
用于训练大语言模型的可验证奖励强化学习（RLVR）通常依赖真实答案来分配奖励，这限制了其在未知真实解法任务中的应用。我们提出了一种排序诱导的可验证框架（RiVER），该框架在没有真实解法的情况下，在基于分数的优化任务上训练大语言模型，使用确定性执行反馈作为连续值监督。当将组相对强化学习应用于此类连续奖励时，我们识别出两个关键挑战：规模主导，即跨测试实例的未校准分数幅度扭曲了策略更新；频率主导，即反复采样的次优解法可能压倒罕见但更强的候选解法。RiVER 通过校准奖励塑造来应对这些挑战，该方法使用实例级比较，强调排名靠前的解法，同时为其他有效解法保留有界反馈。我们在12个AtCoder启发式竞赛任务上进行训练，并在算法工程基准（ALE-Bench）、LiveCodeBench和USACO上进行评估。RiVER 在 ALE 评级排名上将 Qwen3-8B 和 GLM-Z1-9B-0414 分别提升了 8.9% 和 9.4%。更重要的是，尽管仅在基于分数的任务上训练且没有任何真实解法，RiVER 在精确解法基准（如 LiveCodeBench 和 USACO）上也使主干模型平均绝对提升了 2.4% 和 3.5%。相比之下，使用原始执行分数训练的基线模型提升了 ALE 评级，但无法迁移到精确解法基准。这些结果表明，结合适当的奖励校准，基于分数的优化任务可以在没有真实解法的情况下，作为通用编程能力的有效训练环境。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) for training LLMs typically rely on ground-truth answers to assign rewards, limiting their applicability to tasks where the ground-truth solution is unknown. We introduce a \textbf{R}anking-\textbf{i}nduced \textbf{VER}ifiable framework (RiVER) that trains LLMs on score-based optimization tasks without ground-truth solutions, using deterministic execution feedback as continuous-valued supervision. When applying group-relative RL to such continuous rewards, we identify two key challenges: \emph{scale dominance}, where uncalibrated score magnitudes across test instances distort policy updates, and \emph{frequency dominance}, where repeatedly sampled suboptimal solutions can outweigh rare but stronger candidates. RiVER addresses these challenges with calibrated reward shaping that uses instance-wise comparisons and emphasizes top-ranked solvers while retaining bounded feedback for other valid solutions. We train on 12 AtCoder Heuristic Contest tasks and evaluate on Algorithm Engineering Benchmark (ALE-Bench), LiveCodeBench, and USACO. RiVER advances Qwen3-8B and GLM-Z1-9B-0414 by 8.9\% and 9.4\% in ALE rating rank. More importantly, despite training exclusively on score-based tasks without any ground-truth solutions, RiVER also improves the backbones across exact-solution benchmarks such as LiveCodeBench and USACO by an absolute average improvement of 2.4\% and 3.5\%. By contrast, baselines trained with raw execution scores improve ALE rating but fail to transfer to exact-solution benchmarks. These results suggest that score-based optimization tasks, combined with proper reward calibration, can serve as effective training environments for general coding ability without ground-truth solutions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：现有基于可验证奖励的强化学习（RLVR）在训练大语言模型时，通常依赖真实答案（如数学题的标准解、编程题的通过测试）来分配二元奖励（正确/错误）。这严重限制了RLVR在“无真实解”任务中的应用，例如算法设计、组合优化等开放性问题——这些问题存在多个可行解且质量不一，但全局最优解未知或难以计算。
- **核心问题**：能否利用**无真实解的可验证执行反馈**（如可行性检查、目标函数值）作为训练信号，提升LLM的编码与推理能力？
- **整体含义**：论文证明，通过合适的奖励校准（实例级排序+胜者加重塑造），基于分数的优化任务可以成为有效的训练环境，不仅提升目标领域（算法工程竞赛）的性能，还能迁移至传统的通过/失败编程基准（LiveCodeBench、USACO），从而拓展RLVR的适用范围。

### 2. 论文提出的方法论

- **核心思想**：RiVER（Ranking-induced VERifiable framework）将无真实解的优化环境中的连续执行得分，转化为稳定的策略梯度信号。
- **关键技术细节**：
  1. **实例级排序**：对每个隐藏测试实例，将生成的候选解法按目标函数值排序（使用平均秩处理平局），从而消除不同实例间分数尺度差异（解决**规模主导**问题）。
  2. **胜者加重奖励塑造**：对每个解法-实例对，赋予如下奖励：
     - 无效输出：-1；
     - 最佳有效解法：+1；
     - 其他有效解法：根据排名给予 `clip((G−1−r)/(G−3), 0, 1) - 0.5`，即范围[-0.5, 0.5]的有界反馈。
     这使得最佳解法获得突出奖励，同时保留对其他解法的区分度，减少重复次优模式对梯度的主导（解决**频率主导**问题）。
  3. **聚合与GRPO更新**：将各实例的塑造奖励平均作为样本级优势，替代标准GRPO中的二元或原始分数奖励，并保留GRPO的裁剪目标与KL正则化。
- **算法流程**（文字描述）：
  1. 对每个问题提示，从旧策略采样G个候选响应 → 提取代码；
  2. 将每个代码在M个隐藏实例上执行，得到有效性矩阵和目标分数矩阵；
  3. 对每个实例，对有效解法进行平均秩排序；
  4. 根据排序结果，为每个解法-实例对计算上述塑造奖励；
  5. 平均各实例奖励，得到每个解法的优势值；
  6. 执行GRPO更新策略。

### 3. 实验设计

- **训练数据集**：AtCoder Heuristic Contest (AHC) 中的12个任务（AHC047~062子集，排除4个不兼容任务），均为无真实解的分数优化问题。
- **评估基准**：
  - **基于分数的竞赛**：ALE-Bench（AtCoder风格排名）。
  - **精确解法基准**：LiveCodeBench v5 & v6、USACO（按测试用例通过率评估）。
- **对比方法**：
  - **Raw-GRPO**：直接使用原始执行分数作为奖励。
  - **RS-GRPO**：风险敏感变换替代组归一化。
  - **Raw-Binary**：仅胜者得1，其余得0。
  - **Instance-Norm**：实例内归一化原始分数。
  - **Rank-uniform**：实例级排名但奖励均匀分布。
  - **RiVER**（本文方法）。
- **基座模型**：Qwen3-8B 和 GLM-Z1-9B-0414。
- **训练细节**：AdamW优化器，学习率1e-6，KL系数0.001，组大小G=16，全局batch size=64。

### 4. 资源与算力

- **原文未明确说明使用的GPU型号、数量及训练时长**。仅提及优化器与超参数设置（如上）。因此无法准确总结算力消耗。

### 5. 实验数量与充分性

- **实验数量**：主实验（表1）涵盖了2个基座模型 × 6种奖励设计（含RiVER） × 3个基准（ALE-Bench、LiveCodeBench v5/v6、USACO），共计约2×6×4=48个评估结果。此外还包括训练动态分析（图2，12个任务的最佳分数曲线）和定性代码分析（附录C，3个解法的对比）。
- **消融实验**：通过对比Raw-GRPO、Instance-Norm、Rank-uniform等变体，验证了实例级排序和胜者加重塑造各自的必要性。
- **公平性**：训练集与评估基准无重叠（AHC训练任务使用ALE-Bench截止日期后的新任务）；所有方法使用相同训练超参数和计算资源。实验设计较为严谨。
- **充分性**：尽管在多个基准上验证，但仅使用了两个8B/9B规模的模型，缺乏对不同规模模型（如70B）或更多模型家族的验证；训练环境仅限AHC，未覆盖其他无真实解优化领域（如Topcoder）。

### 6. 论文的主要结论与发现

1. **RiVER在无真实解训练下显著提升ALE排名**：Qwen3-8B提升142点（排名百分位下降8.9%），GLM-Z1-9B提升157点（下降9.4%）。
2. **RiVER迁移至精确解法基准**：在LiveCodeBench和USACO上平均提升2.4%和3.5%（绝对），而使用原始分数的基线几乎无迁移增益。
3. **奖励校准至关重要**：原始分数或未校准的排名奖励虽能提升目标领域，但无法泛化；胜者加重奖励塑造是跨域迁移的关键。
4. **困难问题收益更大**：LiveCodeBench上中/难题提升显著，容易题接近饱和。
5. **训练动态显示RiVER在多数任务中发现更强或更早的最优解法**（图2）。

### 7. 优点

- **方法创新**：首次系统研究无真实解的强化学习训练LLM，提出实例级排序+胜者加重奖励塑造，针对性解决连续奖励中的规模主导和频率主导问题。
- **实验设计全面**：同时评估目标领域（分数优化）和迁移领域（精确解法），并包含多种奖励设计消融，结论可信度高。
- **跨域迁移发现**：证明基于分数的优化任务经过适当校准后能提升通用编码能力，为RLVR提供了新的、可扩展的训练数据来源。
- **实践性**：框架对执行环境要求低（仅需可执行评估器），易于集成到现有RL训练流程。

### 8. 不足与局限

- **模型规模单一**：仅测试8B/9B级模型，未在更大模型（如70B、130B）上验证，效果能否放大未知。
- **训练数据局限**：仅使用12个AHC任务，领域偏算法工程，未涵盖其他无真实解优化场景（如数学证明、策略规划），泛化性有待进一步检验。
- **资源消耗未披露**：未提供训练所需的GPU型号、数量及时间，不利于复现与成本评估。
- **推理开销**：每步训练需对每个候选解法执行代码（多实例），计算成本较高；未讨论优化策略。
- **奖励设计依赖G（组大小）**：公式中的奖励边界依赖于G，未研究G的敏感性；平局处理规则对重复模式的有效性可能有限。
- **理论分析较弱**：虽然用信息论视角给出反馈分辨率概念，但未与实验结果建立定量联系，且证明仅针对离散反馈，连续反馈需额外假设。

（完）
