---
title: "Learning with a Single Rollout via Monte Carlo Pass@k Critic"
title_zh: "通过蒙特卡洛Pass@k评判器实现单次轨迹学习"
authors: "Fengdi Che, Yang Liu, Lei Yu, Meng Cao, Tong Che, Rupam Mahmood, Dale Schuurmans"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25451v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 单轮采样PPO用于大语言模型强化学习中的细粒度信用分配
tldr: "为克服语言模型强化学习中重复采样成本高、信用分配困难的问题，提出单轨迹近端策略优化（SR-PPO）。该方法利用一次Rollout的Monte Carlo结果训练token级批评者，以Pass@k成功概率作为学习信号，在数学推理基准上实现稳定训练和Pass@128一致提升，为高效信用分配提供新路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25451v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1428, \"height\": 350, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1366, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1376, \"height\": 901, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25451v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 222, \"label\": \"Table\"}]"
motivation: 现有RL方法在语言模型上面临重复采样成本高和结果奖励稀疏的信用分配难题，亟需高效单轨迹学习方法。
method: "SR-PPO通过单次Rollout的Monte Carlo结果训练token级批评者，以Pass@k概率作为优势信号，避免对比采样并实现细粒度信用分配。"
result: "在HMMT26和AIME24基准上，SR-PPO的Pass@128成功率一致提升，且训练动态稳定。"
conclusion: SR-PPO以单轨迹完成高效信用分配，无需对比样本，为语言模型强化学习提供了实用方案。
---

## 摘要
在语言模型的强化学习（RL）中，估计词元级别的优势仍然具有挑战性，因为扩大情节经验采集的成本很高。对于基线优势估计方法，难度更大，因为重复采样会导致轨迹发散为本质上不同的推理前缀。在这种情况下，诸如GRPO之类的RL算法被证明是有限的：结果奖励过于稀疏，无法归因于中间步骤等具体动作，并且由于采样轨迹的异质性，跨样本的比较并非易事。为了缓解重复采样的计算成本和信用分配困难，我们研究了在语言模型RL中具有词元级信用分配的单次轨迹近端策略优化（SR-PPO）。我们不通过在候选组内归一化情节回报来估计优势，而是使用每次提示单次轨迹的蒙特卡洛结果训练一个校准的词元级信用评判器。具体来说，我们使用评判器来预测提示前缀的Pass@k成功概率，该概率源自Pass@1尝试。这一选择产生的学习信号比Pass@1更具选择性：它降低了易解前缀的权重，同时优先处理成功概率仍然较低的高难度前缀。我们证明，随着k的增加，Pass@k收敛到一个可达性指标，反映了一个前缀是否可能导致至少一个成功的延续。在显式状态图中，极限（k→∞）可以在O(|V|+|E|)时间内计算，为直接信用分配提供了一个有前景的替代方案，无需采样对比轨迹。作为初步验证，SR-PPO表现出稳定的学习动态，以及在数学推理基准（如HMMT26和AIME24）上Pass@128成功率的持续提升。

## Abstract
Estimating token-level advantages in reinforcement learning (RL) for language models remains challenging because scaling up episodic experience collection is expensive. The difficulty intensifies for baseline advantage estimation methods, where repeated sampling causes trajectories to diverge into substantially different reasoning prefixes. In this context, RL algorithms such as GRPO prove limited: an outcome reward is too sparse to be attributed to specific actions like intermediate steps, and comparisons across sampled traces are non-trivial because they are heterogeneous. To mitigate both the computational cost of repeated sampling and the difficulty of credit assignment, we study single-rollout proximal policy optimization (SR-PPO) featuring token-level credit assignment in RL for language models. Instead of estimating advantages by normalizing episodic returns within the candidate group, we train a calibrated token-level credit critic using Monte Carlo outcomes from one rollout per prompt. Specifically, we use the critic to predict the Pass@k success probability at the prompt prefix, which is derived from a Pass@1 attempt. This choice yields a more selective learning signal than Pass@1: it discounts easily solved prefixes while prioritizing hard ones whose success probability remains marginal. We show that as $k$ increases, Pass@k converges to a reachability indicator, reflecting whether a prefix can lead to at least one successful continuation. In an explicit state graph, the limit ($k \rightarrow \infty$) can be computed in $O(|V|+|E|)$ time, offering a promising surrogate for direct credit assignment without the need to sample contrastive traces. As an initial validation, SR-PPO exhibits stable learning dynamics, along with consistent gains in Pass@128 success rates on mathematical reasoning benchmarks such as HMMT26 and AIME24.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：在语言模型（LM）的强化学习（RL）中，结果奖励（outcome reward）过于稀疏，难以分配给中间推理步骤（token级别）。现有方法如GRPO需要采样多个轨迹（per prompt）来获得基线，但多轨迹异质性强、计算成本高，尤其在长周期或智能体系统中。
- **动机**：实现**单次轨迹（single-rollout）**下的有效token级信用分配，避免重复采样，降低成本。
- **核心含义**：提出了SR-PPO，用一次回滚的蒙特卡洛结果训练一个前缀级别的信用批评者（credit critic），预测Pass@k成功概率，从而在单轨迹内生成密集、有选择性的学习信号，实现稳定训练和性能提升。

### 2. 论文提出的方法论
- **核心思想**：训练一个前缀信用模型（critic），从每个前缀预测最终成功的概率（Pass@k值）。将该预测作为值函数，通过前后值之差得到token级优势，配合终端校正项实现细粒度信用分配。
- **关键技术细节**：
  - **信用批评器训练**：对每条轨迹的所有前缀，用同一个二元结果Y监督，损失为BCE + Brier score（提高校准性）。
  - **Pass@k值定义**：\( q_{\pi,k}(s_t) = 1 - (1 - q_{\pi}(s_t))^k \)，其中\( q_{\pi}(s_t) \)是Pass@1成功率。训练时直接预测\( \hat{q}_{\pi,k} \)，再反变换为\( \hat{q}_{\pi} \)用于损失计算。
  - **Token级优势**：\( A_{t} = (\hat{q}_{\pi,k}(s_t) - \hat{q}_{\pi,k}(s_{t-1})) + (Y - \hat{q}_{\pi,k}(s_T)) \)，终端项使累积信号与最终结果对齐。
  - **策略优化**：使用单次PPO更新（每个prompt一个rollout），目标为\( J(\theta) = \frac{1}{M}\sum_i\sum_t r_{i,t}(\theta) A^{(k)}_{i,t} \)。由于每次梯度更新前策略与采样策略相同，clip分支失效，相当于on-policy单步梯度更新。
  - **可达性解释**：当\( k \to \infty \)，Pass@k收敛到二元可达性（是否有成功路径）。在显式状态图上，可达性可在\( O(|V|+|E|) \)时间精确计算，无需采样。论文使用Pass@4作为软替代。
- **算法流程**：对于每个prompt，采样一个完整轨迹；用该轨迹中所有前缀训练批评器（在线学习）；用批评器输出计算每个token的优势；用PPO目标更新策略网络。不依赖组内归一化。

### 3. 实验设计
- **数据集**：训练数据为DeepScaleR数学推理数据集。
- **基准与评估环境**：HMMT26, AIME24, AIME25（竞争数学推理benchmark）。评估时每个问题采样128个完成，报告Pass@k（主要报告Pass@8, Pass@128等）。
- **对比方法**：
  - **GRPO**：标准组相对策略优化（每prompt 8条轨迹）。
  - **GAE-PPO**：标准PPO + 广义优势估计（GAE）。
  - **SR-PPO变体**：Pass@1 SR-PPO, Pass@4 SR-PPO（本文主要选项）。
- **主要结果**：
  - 图1(b)：Pass@4 SR-PPO与GRPO在验证Pass@8上表现相当，但SR-PPO仅用单轨迹（GRPO用8倍rollout）。
  - 图2：GAE-PPO在单轨迹下几乎不学习，格式分数也低；SR-PPO稳定提升。
  - 图3：在AIME24/25, HMMT26上，Pass@4 SR-PPO的Pass@k随k线性增长，性能优于GRPO（step 120）或持平。
  - 图1(c)：Pass@4 SR-PPO在Pass@1上可能不如Pass@1 SR-PPO，但后者不稳定（尤其梯度累积时）。
  - 表1：Pass@4 SR-PPO的token优势稀疏性远高于Pass@1（49%-59% vs 0.61%的token优势幅值<0.01），说明信号更有选择性。

### 4. 资源与算力
- **未明确说明**：论文未提及GPU型号、数量、训练时长等具体算力资源。仅提到使用Qwen3-1.7B（1.7B参数模型），最大响应长度9182 tokens，batch size为256 prompts（SR-PPO），GRPO为128 prompts×8 rollouts。无法评估计算开销的具体规模。

### 5. 实验数量与充分性
- **实验数量**：多组实验覆盖多个基准（AIME24/25, HMMT26），包括主要对比（图1-3）、消融（Pass@1 vs Pass@4，梯度累积影响）、稀疏性分析（表1）、定性可视化（附录C）。实验比较充分。
- **充分性**：验证了单轨迹方法的有效性和稳定性，解释了Pass@k的优势。但局限性在于：
  - 仅使用1.7B参数模型（Qwen3-1.7B）；
  - 仅在一个数据集（DeepScaleR）上训练；
  - 仅使用一个随机种子（a single running seed）；
  - 超参数调优有限（仅对GRPO调了学习率）。
- **客观性**：实验对比设置公平（如GRPO使用更多rollout但仍被赶超），但未在更大模型或更多场景验证，结果可能受限于小模型。

### 6. 论文的主要结论与发现
- 单轨迹PPO（SR-PPO）通过前缀级别的Pass@k信用批评器，可以实现有效且稳定的训练，在数学推理任务上提升Pass@128成功率。
- Pass@4（而非Pass@1）作为学习信号更优：它产生更稀疏、更有选择性的token级优势，抑制易解前缀的更新，聚焦难但可达的前缀，从而提高训练稳定性。
- Pass@k在k→∞时与可达性关联，提供一种无需采样多条轨迹的信用分配替代思路。
- GAE-PPO在单轨迹环境下失败，说明传统TD引导的优势估计不适合稀疏延迟奖励的场景。

### 7. 优点
- **方法创新**：将单轨迹信用分配与Pass@k值学习结合，兼具计算效率与信用精度。
- **理论连接**：揭示了Pass@k与可达性的关系，提供了从概率到支持（support）的自然过渡，并给出线性时间可达性计算的算法（算法1）。
- **实用性强**：无需多轨迹采样，适用于高成本、长周期或异质轨迹的智能体训练。
- **实验验证充分**：多项对比和消融支持论点，特别是稀疏性分析直观验证了Pass@4的选择性优势。

### 8. 不足与局限
- **实验覆盖有限**：仅在一个数学推理数据集和一个1.7B参数模型上评估；未在更大模型（如7B/70B）、其他领域（代码、智能体）或长周期任务上验证。
- **随机种子单一**：仅一个种子结果，统计显著性未充分保证。
- **超参数敏感度未知**：未探索学习率、KL系数、温度、k值等其他选择的影响，尤其是k>4的表现。
- **可达性联系为概念性**：实际训练中并不显式计算可达性，而是用Pass@4作为代理，理论到实践的桥梁不够坚固。
- **缺乏计算资源说明**：无法独立复现或评估成本。

（完）
