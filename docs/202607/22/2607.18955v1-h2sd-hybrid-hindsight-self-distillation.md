---
title: "H$^2$SD: Hybrid Hindsight Self-Distillation"
title_zh: "H$^2$SD: 混合事后自我蒸馏"
authors: "Qiye Cai, Yichuan Ma, Linyang Li, Peiji Li, Yongkang Chen, Qipeng Guo, Yicheng Zou, Tao Gui, Xiaocheng Feng, Bing Qin"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18955v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 混合事后自蒸馏，针对RLVR中的在线策略蒸馏
tldr: 基于可验证奖励的强化学习（RLVR）在提升语言模型推理能力时，因仅给整个轨迹标量奖励导致监督稀疏且信用分配受限。现有自蒸馏方法如OPSD存在信息泄露，RLSD缺乏明确纠正方向。本文提出混合后见自蒸馏（H²SD），对成功轨迹用教师响应调节更新幅度，对失败轨迹利用参考提示最小化反向KL散度。在多个推理基准上，H²SD一致优于RLVR、OPSD和RLSD基线，且优化稳定、生成高效。该方法为语言模型推理能力提升提供了有效的密集监督方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18955v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 725, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1555, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 739, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 735, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18955v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1523, \"height\": 478, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18955v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1726, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18955v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18955v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 849, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18955v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 849, \"height\": 283, \"label\": \"Table\"}]"
motivation: 现有RLVR监督稀疏，自蒸馏方法存在信息泄露或缺乏纠正方向，需更有效的密集监督方法。
method: 根据轨迹正误差异化使用教师：成功时用教师概率调节更新幅度，失败时基于参考提示最小化反向KL散度。
result: 在多个推理基准上，H²SD一致超越RLVR、OPSD和RLSD，提升准确率和训练稳定性。
conclusion: H²SD通过混合后见自蒸馏实现了密集有效监督，为语言模型推理能力增强提供了实用方案。
---

## 摘要
带有可验证奖励的强化学习（RLVR）在数学推理和代码生成等任务上显著提升了大语言模型的推理能力。然而，大多数RLVR方法为整个轨迹分配一个标量结果奖励，导致监督稀疏且令牌级信用分配受限。在线策略蒸馏（OPD）通过从更强的教师模型中蒸馏令牌级分布提供更密集的监督，但需要额外的教师，并通常假设共享词汇表。在线策略自我蒸馏（OPSD）通过在同一模型上以特权信息为条件构建教师策略来消除这一依赖。然而，直接匹配教师分布可能导致信息泄露和优化不稳定。RLSD通过仅使用教师信号调节更新幅度来避免直接匹配，但无法在采样推理失败时提供显式的纠正方向。为了解决这一权衡，我们提出H²SD，一种混合事后自我蒸馏框架，根据轨迹正确性不同地使用教师。对于成功轨迹，教师接收被确认为正确的学生响应以及改写指令，其对原始响应令牌的概率用于调节更新幅度，而不改变由奖励确定的方向。对于失败轨迹，我们以包含关键推理步骤和已验证答案的参考提示为条件，并最小化学生到教师的逆KL散度。在多个具有挑战性的推理基准上的实验表明，H²SD一致优于代表性的RLVR、OPSD和RLSD基线，同时保持稳定的优化和良好的生成效率。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has substantially improved the reasoning capabilities of large language models on tasks such as mathematical reasoning and code generation. However, most RLVR methods assign a scalar outcome reward to an entire trajectory, resulting in sparse supervision and limited token-level credit assignment. On-policy distillation (OPD) provides denser supervision by distilling token-level distributions from a stronger teacher model, but requires an additional teacher and typically assumes a shared vocabulary. On-policy self-distillation (OPSD) removes this dependency by conditioning the same model on privileged information to construct a teacher policy. However, directly matching the teacher distribution may cause information leakage and unstable optimization. RLSD avoids direct matching by using the teacher signal only to modulate update magnitudes, but it cannot provide an explicit correction direction when the sampled reasoning fails. To address this tradeoff, we introduce $\mathrm{H}^{2}\mathrm{SD}$, a hybrid hindsight self distillation framework that uses the teacher differently according to trajectory correctness. For successful trajectories, the teacher receives the student response confirmed as correct together with a rephrasing instruction, and its probabilities on the original response tokens are used to modulate update magnitudes without changing the direction determined by the reward. For failed trajectories, we condition the teacher on a reference hint containing key reasoning steps and a verified answer, and minimize the reverse KL divergence from the student to the teacher. Experiments on multiple challenging reasoning benchmarks show that H$^2$SD consistently outperforms representative RLVR, OPSD, and RLSD baselines while maintaining stable optimization and favorable generation efficiency.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：H²SD: Hybrid Hindsight Self-Distillation

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：基于可验证奖励的强化学习（RLVR）在提升大语言模型（LLM）推理能力方面取得了显著成功，典型代表为GRPO。但RLVR通常只给整个轨迹一个标量结果奖励，导致监督稀疏（sparse supervision），无法在令牌级别（token-level）进行精细的信用分配（credit assignment）。
- **现有方法的不足**：
  - 在线策略蒸馏（OPD）通过教师模型提供密集的令牌级分布监督，但需要额外更强教师，且通常要求共享词汇表，限制应用。
  - 在线策略自我蒸馏（OPSD）用同一模型作为师生，通过特权信息构造教师，但直接匹配分布会导致信息泄露和训练不稳定。
  - RLSD 只利用教师信号调节更新幅度，避免了直接匹配，但在采样失败时无法提供显式纠正方向，仅改变惩罚强度。
- **核心问题**：如何在保持训练稳定的同时，为RLVR提供有效的密集监督，既能精细信用分配，又能纠正错误推理路径。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **Hybrid Hindsight Self-Distillation (H²SD)**：根据轨迹的正确性（是否成功）采用不同的教师使用策略。
- **关键洞察**：成功轨迹已有可靠的奖励方向，只需精细调整信用分配；失败轨迹需要显式的分布纠正。

### 技术细节
- **提示（Hint）生成**：使用一个更强的外部语言模型（如Kimi-K2.6）为每个问题生成一个自然语言参考提示（hint），包含关键中间推理步骤和经验证的最终答案。此提示仅作为特权信息用于训练，不直接作为教师。
- **成功轨迹处理**：
  - 教师接收学生已被验证正确的响应，加上一条“rephrase”指令（改写指令）。
  - 教师对该响应原始令牌的概率用于计算RLSD中的幅度权重 \( w_t = \exp(\text{sign}(A) \cdot \Delta_t) \)，其中 \(\Delta_t = \text{sg}(\log P_T(y_t) - \log P_S(y_t))\)。
  - 最终令牌级优势 \(\hat{A}_t = A[(1-\lambda) + \lambda \cdot \text{clip}(w_t, 1-\epsilon_w, 1+\epsilon_w)]\)，仅调节更新幅度，不改变奖励方向。
- **失败轨迹处理**：
  - 教师以参考提示和问题为条件，计算分布 \( P_T^{hint} \)。
  - 最小化学生到教师的反向KL散度：\(\mathcal{L}_{\text{RKL}} = \frac{1}{T}\sum_{t=1}^T D_{\text{KL}}(p_{t,\theta}^S \| \text{sg}[p_{t,\theta}^{T,hint}])\)。
  - 提供明确的纠正方向。
- **总体目标**：\(\mathcal{L}_{\text{H²SD}} = \mathbb{E}[m \mathcal{L}_{\text{RLSD}} + \gamma (1-m) \mathcal{L}_{\text{RKL}}]\)，其中 \(m\) 是轨迹正确性的指示变量（1成功，0失败）。

### 算法流程（文字说明）
1. 从当前策略采样多个响应。
2. 使用验证器计算每个响应的奖励，分组计算相对优势。
3. 对每个轨迹：
   - 若成功（R=1），使用教师（条件为“rephrase”指令+学生响应）计算幅度权重，按RLSD方式更新。
   - 若失败（R=0），使用教师（条件为参考提示+问题）计算分布，优化反向KL散度。
4. 组合两种损失进行更新。

## 3. 实验设计

### 数据集/场景
- **三种逻辑推理基准**：
  - **Sudoku**：6×6 和 8×8 网格。
  - **Calcudoku**：5×5 和 6×6 网格，加入区域算术约束。
  - **Arrow Maze**：6×6、7×7、8×8、9×9、10×10 共五种尺寸。
- 训练集和验证集划分：论文未详细说明具体样本数，但每个基准包含多个难度规模。

### 对比方法
- **基模型**：Qwen3-30B-A3B-Instruct-2507。
- **对比基线**：
  - GRPO（原始RLVR）
  - SDPO（基于兄弟正确响应的自我蒸馏）
  - RLSD（仅幅度调节的自蒸馏）
  - OPSD（直接匹配教师分布）
  - SRPO（成功轨迹用GRPO，失败用SDPO）
  - RLSD+hint（RLSD使用相同提示作为特权信息，隔离提示效果）
- 所有方法均使用相同的基模型和相同的特权信息（提示）来源（除GRPO、RLSD原版等）。

## 4. 资源与算力

- **明确说明**：训练使用4个节点，每个节点配备8块NVIDIA H200 140GB GPU，共32块GPU。
- **训练时长**：论文未明确给出每个实验的具体训练时长，但提到了“rollout steps”的消融图（最多约600步），未说明收敛所需总步数。
- **优化实现**：使用VERL框架和SGLang进行推理加速。OPSD为了减少显存占用，使用了top-100令牌概率保留+尾部分布合并的策略。

## 5. 实验数量与充分性

### 定量实验
- **主结果表（Table 1）**：涵盖Sudoku两类、Calcudoku两类、Arrow Maze五类，共9个设置，对比了7种基线方法（包括基模型），总计72个数据点。
- **消融实验**：
  - 路由策略消融（Table 2）：比较“仅幅度”、“仅反向KL”、“反向路由”和H²SD。
  - 特权信息类型消融（Table 3）：比较程序反馈、正确答案、兄弟正确响应、外部提示四种信息。
  - 改写策略消融（Table 4）：在三种更新策略下评估有无rephrase的效果。
  - 熵动力学分析（Figure 4, 5）：展示H²SD与反向路由的熵变化，以及重述对熵的影响。
  - 准确率-生成成本权衡（Figure 3）。

### 充分性与客观性
- 实验覆盖多个逻辑推理任务和多种网格尺寸，包括迁移测试（如Arrow Maze的无见面尺寸）。
- 消融实验系统性地验证了核心设计选择（路由、特权信息类型、改写）。
- 对比基线均为近期代表性方法，且H²SD与OPSD、RLSD使用完全相同的提示，公平隔离提示本身的影响。
- 不足之处：缺少与其他复杂推理任务（如数学、代码生成）的直接对比；未报告多次运行的方差或使用统计显著性测试；消融实验仅在部分任务上进行（Sudoku为主），未在所有任务上验证所有消融。

## 6. 论文的主要结论与发现

1. H²SD在所有基准任务上取得了最佳整体性能（Overall 50.49%，远超第二好的RLSD+hint 24.72%），尤其在Sudoku上提升巨大。
2. 成功轨迹使用幅度调制、失败轨迹使用反向KL蒸馏的组合，优于任何单一策略。
3. 外部生成的提示（hint）提供的处理级信息比仅依赖最终答案或程序反馈更有效。
4. 重述（rephrasing）在幅度调制策略中有显著益处，但在反向KL蒸馏中反而有害，因为过度约束正确轨迹。
5. H²SD的改进并非通过生成更长响应获得，实际上生成成本更低。
6. 正确的路由（根据轨迹正确性）是维持探索、避免熵崩塌的关键。

## 7. 优点

- **方法设计创新**：首次根据轨迹正确性差异化地使用教师信号，既利用了成功轨迹的稳定方向，又为失败轨迹提供显式分布纠正。
- **缓解信息泄露**：在成功轨迹上避免直接匹配教师分布，降低模型依赖特权信息；失败轨迹上的反向KL也因教师分布不参与梯度而保持稳定。
- **框架通用性**：不要求师生共享词汇表（外部提示生成器可以是任意模型），且仅需单次训练，无需额外教师持续运行。
- **实验全面且公平**：对比基线充分（包括GRPO、OPSD、RLSD及其变体），且隔离了提示本身的影响，证明收益来源与目标设计而非数据增强。
- **效率优良**：在提升准确率的同时降低了推理阶段生成成本。

## 8. 不足与局限

- **任务覆盖有限**：当前仅在逻辑推理任务（Sudoku、Calcudoku、Arrow Maze）上验证，未在更多通用推理任务（如数学、代码、科学问答）或语言生成任务上实验，泛化性尚未完全证明。
- **依赖外部提示生成器**：需要额外更强的模型（如Kimi-K2.6）来生成参考提示，引入计算开销和外部依赖（虽然仅在训练阶段使用）。
- **潜在偏差风险**：
  - 提示生成器可能自身有偏差，其推理过程可能不完全正确或存在特定风格，若提示有错误，可能误导失败轨迹的纠正方向（但论文在Table 3显示提示优于其他选项）。
  - 成功轨迹使用的重述指令也可能改变语义，论文通过消融实验证明重述有益，但未深入分析重述质量对结果的影响。
- **训练动态稳定性分析**：虽展示了熵图，但未对不同学习率、gamma系数（\(\gamma\)）、RLSD的\(\lambda\)等超参数进行系统灵敏度分析。
- **计算资源需求较高**：32块H200 GPU的配置对多数研究团队而言门槛较高，且未讨论小规模模型或资源受限场景下的表现。
- **未报告统计显著性**：实验结果为单次运行（推测），未提供标准差或多次实验的平均值，难以评估改进的稳定性。

（完）
