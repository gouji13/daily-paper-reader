---
title: Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning
title_zh: 单次推步异步优化用于智能体强化学习
authors: "Zhenyu Hou, Yujiang Li, Jie Tang, Yuxiao Dong"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07508v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 智能体强化学习、异步优化、大模型后训练
tldr: 强化学习（RL）正用于大语言模型后训练，但同步批处理在长程代理任务中效率低。异步RL虽吞吐高，却存在稳定性和off-policy问题。本文提出单rollout异步优化（SAO），以单rollout采样替代组采样，结合双面token级裁剪和实用价值模型训练，降低off-policy效应并提升稳定性。在SWE-Bench等基准上，SAO稳定训练千步并超越GRPO及其变体，成功部署于750B参数的GLM-5.2模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07508v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1342, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1011, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1420, \"height\": 414, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1422, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 644, \"height\": 426, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07508v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1396, \"height\": 655, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 575, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 761, \"height\": 218, \"label\": \"Table\"}]"
motivation: 现有异步RL在代理任务中缺乏稳定性，GRPO的组采样不适应异步训练，导致off-policy问题突出。
method: 提出单rollout异步优化（SAO），用单rollout采样替代组采样，并引入双面token级裁剪和实用价值模型训练。
result: 在SWE-Bench Verified、BeyondAIME等基准上稳定训练千步，一致超越GRPO及其变体，并成功部署于750B参数的GLM-5.2模型。
conclusion: 单rollout RL在动态环境中有效，为异步代理RL提供了稳定高效的训练方案。
---

## 摘要
强化学习（RL）在大语言模型后训练中日益重要。以往针对大语言模型的强化学习流程大多是同步和批交错的，这不利于长周期智能体任务。最近，异步强化学习作为一种更高效的方案出现，通过随推步到达更新模型。然而，现有异步强化学习系统往往强调吞吐量，而对训练稳定性和任务有效性的探索不足。例如，一个关键挑战是广泛使用的GRPO框架中的分组采样天然不适用于异步智能体训练。本文提出单次推步异步优化（SAO）来解决异步强化学习中的稳定性和离线策略挑战。为减少离线策略影响并提升泛化能力，我们用单次推步采样替代分组采样，即每个提示仅使用一次推步。我们进一步通过实用的价值模型训练设计改进了这种单次推步策略。为提升优化稳定性，我们引入了严格的双侧符元级裁剪策略。SAO能够稳定训练一千步，并在智能体编程与推理基准（如SWE-Bench Verified、BeyondAIME和IMOAnswerBench）上持续优于GRPO及其变体。我们还证明，在模拟在线学习环境中，即模型必须适应不断变化的环境时，单次推步强化学习特别有效。为此，SAO已成功部署在智能体强化学习流程中，用于训练开源GLM-5.2模型（750B-A40B）。

## Abstract
Reinforcement learning (RL) is becoming increasingly important for post-training large language models (LLMs). Previous RL pipelines for LLMs were mostly synchronous and batch-interleaved, which is inefficient for long-horizon agentic tasks. Recently, asynchronous RL has emerged as a more efficient alternative by updating the model as rollouts arrive. However, existing asynchronous RL systems often emphasize throughput, while leaving training stability and task effectiveness largely underexplored. For example, a key challenge is that group-wise sampling in the widely-used GRPO framework does not naturally fit asynchronous agentic training. In this paper, we present Single-rollout Asynchronous Optimization (SAO) to address the stability and off-policy challenges in asynchronous RL. To reduce off-policy effects and improve generalization, we replace group-wise sampling with single-rollout sampling, that is, using one rollout per prompt. We further improve this single-rollout strategy with practical value-model training designs. To improve optimization stability, we introduce a strict double-side token-level clipping strategy. SAO is able to train stably for one thousand steps and consistently outperform GRPO and its variants on agentic coding and reasoning benchmarks, such as SWE-Bench Verified, BeyondAIME, and IMOAnswerBench. We also demonstrate that single-rollout RL is particularly effective in a simulated online learning setting, where the model must adapt to changing evolving environments. To this end, SAO is successfully deployed in the agentic RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大语言模型（LLM）后训练中，强化学习（RL）日益重要。传统同步批处理RL（如PPO、GRPO）在生成长周期智能体任务（如编程、多轮推理）时，由于轨迹长度不一，导致大量GPU空闲等待“最慢”的推步，效率低下。
- **问题**：异步RL虽然能提升吞吐量，但引入了**策略滞后（policy lag）**和严重的**离策略（off-policy）问题**，训练稳定性差；同时，广泛使用的**分组采样**（如GRPO）因需等待同组所有完成才能训练，与异步环境不兼容，且不适用于每条提示仅返回单条轨迹的在线/动态环境。
- **含义**：亟需一种既保持异步效率，又能确保训练稳定性和任务有效性的算法框架。

### 2. 方法论：核心思想、关键技术细节

- **方法名称**：**单次推步异步优化（Single-rollout Asynchronous Optimization, SAO）**  
- **核心思想**：用**单次推步采样**替代分组采样，并配合稳健的价值模型训练和严格的裁剪策略，以缓解离策略效应并稳定训练。

- **关键技术细节**：
  1. **双面重要性采样（DIS, Double-sided Importance Sampling）**  
     - 直接用 `π_rollout` 作为行为策略，计算概率比 `r_t(θ) = exp( logπ_θ - logπ_rollout )`，省略不准确的旧策略 `π_θ_old`，降低计算开销。  
     - 采用**双面裁剪+掩码**：仅保留 `1-ϵ_l < r_t < 1+ϵ_h` 内的token，其余token梯度设为0，防止极端离策略更新。
  2. **单次推步训练价值模型**  
     - **更频繁的价值更新（Faster Value Update）**：每个策略更新步骤，价值网络更新K次（实验中K=2），使其更快适应策略变化，降低方差。  
     - **冻结注意力（Frozen-Attention）**：价值模型训练时冻结全注意力层参数，仅优化MoE层，避免梯度爆炸（源于注意力层梯度大）。  
     - **缩放价值预训练（Scaling Value Pretraining）**：增大价值模型预训练数据量，解决“冷启动”问题。
  3. **跳过观测的token级GAE（Skip-Observation GAE）**  
     - 处理智能体多轮轨迹 `[a0, o0, a1, o1, ...]`：将环境反馈（观测）视作非模型生成部分，优势估计仅跨动作边界计算（从 `a_i` 最后一个token到 `a_{i+1}` 第一个token），避免引入环境噪声。
  4. **优化目标**：  
     `L(θ) = E_t[ f(r_t(θ), ϵ_l, ϵ_h) * A_t * log π_θ(a_t|s_t) ]`，其中 `f` 为双面裁剪与掩码函数。

### 3. 实验设计：数据集、基准、对比方法

- **任务**：智能体编程与数学推理。
- **数据集/基准**：
  - 推理：**AIME2025、BeyondAIME、HMMT Nov 2025、IMOAnswerBench**（均支持Python工具调用）
  - 编程：**SWE-Bench Verified**
  - 在线学习模拟：自定义写作风格偏好切换任务（Cute / Chuunibyou / Classical）
- **基准方法**：
  - **基线**：Qwen3-30B-A3B 思维模型（SFT版本）
  - **对比方法**：标准GRPO、带DIS的GRPO（GRPO w/ DIS）、VAPO（无DIS）、运行均值基线（Running mean）、步级GAE变体等
- **评价指标**：Pass@1 准确率（推理时 temperature=1.0，top-p=1.0，多次运行取平均）

### 4. 资源与算力

- 论文未明确给出具体GPU型号、数量及训练时长。仅提及：
  - 使用 **Qwen3-30B-A3B**（300亿参数，MoE结构）进行训练。
  - 最终成功部署于 **GLM-5.2模型（750B-A40B）** 的智能体RL流程中。
  - 训练批量大小为128，最大生成长度128k tokens，策略学习率1e-6，价值学习率5e-6。
- 需要指出：**论文未报告具体的GPU硬件配置（如A100/H100数量）和总训练时间**，因此无法量化算力开销。

### 5. 实验数量与充分性

- **实验组数**：
  - 主实验：在5个不同基准上对比SAO、GRPO及其变体，报告均值（AIME等16次运行，BeyondAIME 4次）。
  - 消融实验（表3、4）：分别验证快速价值更新、冻结注意力、无DIS的VAPO、运行均值基线、步级GAE等。
  - 训练动态分析（图4）：解释方差、裁剪比率、价值梯度范数随步骤变化。
  - 在线学习模拟实验（图5）：展示风格切换后的适应速度。
- **充分性与公平性**：
  - 实验设计较充分，覆盖多种推理和编程场景，消融实验几乎逐一验证每个设计组件。
  - 对比方法包含当前主流的GRPO及其改进版本，且都使用了相同的基础模型（Qwen3-30B-A3B）。
  - 评价时多次运行取平均，降低了单次随机性导致的偏差。
  - 但**未与其他异步RL系统（如AReaL、ROL Flash）进行直接对比**，也未在更小模型上验证，略显不足。

### 6. 主要结论与发现

- SAO在所有测试基准上**一致优于**基线SFT模型和GRPO（含改进版），且能在约1000步内稳定训练不崩溃。
- 单次推步策略（配合价值模型）比分组采样更适应在线动态环境，在写作风格切换实验中能快速适应新偏好。
- **DIS（双面重要性采样）** 是稳定训练的关键：无DIS的VAPO在约90步即崩溃，而GRPO加DIS后训练稳定；但SAO（单次推步+完整设计）仍优于GRPO w/ DIS。
- 快速价值更新和冻结注意力有效提升了价值模型训练的稳定性与解释方差。
- 跳过观测的token级GAE优于步级GAE或运行均值基线。

### 7. 优点

- **方法创新**：首次系统地在智能体异步RL中引入单次推步+双面裁剪，有效解决了策略滞后和离策略问题。
- **实用设计**：冻结注意力、快速价值更新、缩放价值预训练等技巧简洁有效，易于实现。
- **稳定性突出**：可稳定训练上千步，而标准GRPO在约160步后性能崩溃。
- **在线学习能力**：单次推步天然适配仅有单条反馈的环境，实验验证了其相比运行均值基线的优势。
- **部署验证**：成功部署于750B参数级模型GLM-5.2，证明方法在大规模上可行。

### 8. 不足与局限

- **实验覆盖有限**：仅测试了30B级别的Qwen模型，结论可能**不直接适用于小模型或非智能体RLHF场景**（如对话偏好优化）。
- **依赖条件**：需要可靠的**价值模型**和**推步日志概率**，部署时要求基础设施能精确保存异步生成中的token级行为概率。
- **环境单一**：在线学习实验为**受控模拟**（写作风格偏好切换），真实用户在线环境可能更复杂，需更强的安全监控和隐私保护。
- **对比不足**：未与近年其他异步RL系统（AReaL、ROLL Flash）进行直接比较，难以确定纯算法优势与系统级差异。
- **算力细节缺失**：未报告训练所需的GPU型号/数量/总时间，不利于读者评估成本与可复现性。

（完）
