---
title: "Agentic-DPO: From Imitation to Agentic Policy Optimization on Expert Trajectories"
title_zh: "Agentic-DPO: 从模仿到专家轨迹上的智能体策略优化"
authors: "Yixiong Chen, Alan Yuille"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10601v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向LLM智能体的离线策略优化方法
tldr: "针对LLM agent从专家轨迹监督微调仅模仿动作序列而无法区分正确与错误动作的问题，提出Agentic-DPO方法，通过将专家轨迹转化为状态级别的动作偏好，在每一步采样当前模型动作作为负例，使用DPO损失对比学习。引入Policy-Preserving Augmentation避免策略与模式混淆。无需在线环境交互或奖励模型，在多个benchmark上显著超越SFT，tau-bench准确率从21.7%提升至41.4%，证明离线偏好学习可有效优化agent策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10601v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1433, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1413, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 392, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10601v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 986, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 591, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 539, \"height\": 200, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1410, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1447, \"height\": 158, \"label\": \"Table\"}]"
motivation: 现有SFT训练agent仅模仿专家动作序列，无法选择正确动作对抗错误；在线RL成本高，需环境交互和奖励模型。
method: Agentic-DPO将专家轨迹转化为状态条件偏好，在每个状态采样一步当前模型动作作为负例，与专家动作进行DPO对比学习，并用PPA保持策略不变。
result: "在StableToolBench等基准上一致提升，tau-bench上9B模型从21.7%提升至41.4%，匹配在线GRPO。"
conclusion: 专家轨迹可支持低成本agent策略优化，通过转化为状态级动作偏好实现。
---

## 摘要
大型语言模型（LLM）智能体通常通过监督式微调（SFT）从专家轨迹中训练，该方法将多轮智能体行为视为普通文本模仿。这种方法简单且成本低，但只学习模仿专家动作序列，而非训练智能体在每个状态下针对可能的错误选择正确动作。现有缓解此问题的方法包括偏好学习或强化学习，但通常需要高成本的环境交互和奖励模型。我们提出Agentic-DPO，一种轻量级的离线智能体策略优化方法，将专家轨迹转化为状态条件化的偏好监督。在每个专家动作状态，Agentic-DPO从当前状态采样一步动作，将可能的错误动作视为负例，并使用DPO风格的偏好目标将其与专家动作进行对比。为避免在偏好学习中混淆策略和模式，我们引入了策略保持增强（PPA），它在保持专家策略固定的情况下，在多种模式下呈现相同的潜在轨迹。Agentic-DPO无需在线环境交互、奖励模型或全轨迹学生探索。我们在StableToolBench、tau-bench零售和Mind2Web上进行实验，Agentic-DPO在不同模型尺度上持续改进智能体，超越模仿。特别地，对于9B模型，它将tau-bench准确率从21.7%（SFT）提升到41.4%，在相同骨干网络下，仅通过步骤级交互且梯步过程中无需环境交互，即匹配了在线GRPO。结果表明，将专家演示转化为状态级动作偏好后，专家轨迹可以支持低成本的智能体策略优化。Agentic-DPO代码已发布在https://github.com/Schuture/Agentic-DPO。

## Abstract
Large Language Model (LLM) agents are commonly trained from expert trajectories using supervised fine-tuning (SFT), which treats multi-turn agent behavior as ordinary text imitation. This recipe is simple and low-cost, but it only learns to imitate the sequence of expert actions, rather than training the agent to choose the right action against plausible mistakes at each state. Existing methods to mitigate this problem include preference learning or reinforcement learning, but they usually need high-cost environment rollouts and reward models. We propose Agentic-DPO, a lightweight offline agent policy optimization method that turns expert trajectories into state-conditioned preference supervision. At each expert action state, Agentic-DPO samples a one-step action from the current state, treats plausible wrong actions as negatives, and contrasts them with the expert action using a DPO-style preference objective. To avoid mixing both policy and schema in preference learning, we introduce Policy-Preserving Augmentation (PPA), which renders the same latent trajectory under multiple schemas while keeping the expert policy fixed. Agentic-DPO requires no online environment rollout, reward model, or full-trajectory student exploration. We conduct experiments across StableToolBench, tau-bench retail, and Mind2Web, where Agentic-DPO consistently improves agents at different model scales beyond imitation. In particular, it raises tau-bench accuracy from 21.7% (SFT) to 41.4% for a 9B model, matching online GRPO under the same backbone with only step-level rollouts and without environment interaction during gradient steps. The results suggest that expert trajectories can support low-cost agentic policy optimization when converted from demonstrations into state-level action preferences. Code for Agentic-DPO is released at https://github.com/Schuture/Agentic-DPO.

---

## 论文详细总结（自动生成）

# Agentic-DPO：从模仿到专家轨迹上的智能体策略优化——详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：当前LLM智能体通常通过监督微调（SFT）从专家轨迹中训练，但SFT仅模仿动作序列，无法学习在每个状态下区分正确动作与可能错误动作。虽然偏好学习或强化学习能解决此问题，但需要昂贵的环境交互、奖励模型或全轨迹探索，成本高。
- **核心问题**：如何仅利用离线专家轨迹，以低成本构造状态级别的动作偏好，使智能体不仅能模仿专家，还能学会规避自身可能犯的错误。
- **整体含义**：提出Agentic-DPO方法，将专家轨迹中的每一步转换为状态条件偏好（专家动作 vs 学生采样错误动作），通过DPO式对比学习优化策略，无需在线环境交互、奖励模型或全轨迹学生探索，在多个agent基准上超越SFT并匹配在线RL性能，证明离线偏好学习可有效优化agent策略。

## 2. 论文提出的方法论

### 核心思想
- 将专家轨迹中的每个动作视为当前状态的偏好动作，同时在该状态采样一步当前学生策略的动作作为负例，构造偏好对，使用DPO损失优化。
- 引入策略保持增强（PPA）和SFT锚点来稳定训练，避免策略与渲染模式混淆。

### 关键技术细节
- **状态条件动作偏好构造**：对于专家轨迹中的每一步(st, u⁺)，使用当前学生策略πθ_student采样K=4个候选动作，排除格式无效或与专家等价的候选，选择对数概率最高的不同动作作为负例u⁻，形成偏好三元组。
- **Agentic-DPO损失**：基于DPO损失，但使用长度缩放偏好系数βeff = β / max(|a⁺|,|a⁻|)^α (α=0.5)，防止长动作因token数多而主导梯度。
- **SFT锚点**：先进行SFT预热，然后在Agentic-DPO训练中加入SFT损失项（L_total = L_ADPO + λ L_SFT，λ=0.5），保持专家动作概率不被过度压低。
- **策略保持增强（PPA）**：对相同潜在决策在多个渲染视角下呈现，包括动作渲染重写（ReAct风格、JSON列表、重命名工具名等）和状态上下文重写（注入历史错误与修正上下文），使偏好梯度聚焦于潜在决策而非表面形式。
- **刷新轮机制**：每轮使用当前学生重新采样负例，运行R=5轮（小模型需要多轮，9B模型一轮即可达最优）。

### 算法流程（文字描述）
1. 在专家轨迹上使用SFT预热（同时应用在线PPA），冻结参考策略πref。
2. 循环R轮：
   - 从当前学生策略采样K=4个候选动作，过滤后选择最难负例u⁻。
   - 对每批数据，随机采样PPA视角渲染状态和动作，计算Agentic-DPO损失与SFT损失的加权和，更新策略参数。
3. 返回最终策略。

## 3. 实验设计

### 数据集与场景
- **StableToolBench**：工具使用场景（3,786个训练样本），评估标准准确率和扰动准确率（9种扰动，包含2种与PPA重叠、7种完全未见）。
- **τ-bench retail**：长程用户交织工具交互场景（3,786个训练样本），评估任务成功率。
- **Mind2Web**：Web GUI grounding场景（7,362个训练样本），评估跨任务、跨网站、跨域的平均步成功率。
- **BFCL-v3（零样本OOD评估）**：使用ToolACE语料训练，评估函数调用能力。

### 基线方法
- **Base**：不经过任何训练的基础模型
- **SFT**：标准监督微调
- **PPA+SFT**：仅使用PPA增强的SFT（无偏好对比）
- **DFT**：奖励校正SFT变体
- **ETO**：轨迹级DPO（全成功/失败轨迹对比）
- **GRPO**：在线RL（分组轮动，仅用于StableToolBench和τ-bench，因Mind2Web缺乏步级奖励）

### 模型规模
- Qwen3.5-2B/4B/9B
- Gemma3-4B（部分消融实验）

### 实验数量与充分性
- 三大benchmark全覆盖，每个结果3个随机种子平均。
- 主要结果表（Table 1）涵盖所有基线与模型规模，共3×3个表格块。
- 消融实验（Table 2）：对4个组件（SFT预热、SFT锚点、动作渲染PPA、恢复上下文PPA）进行逐一剔除，在2B和Gemma3-4B上验证。
- 鲁棒性实验（Table 3）：区分与PPA重叠的2种扰动和7种未见扰动，验证泛化性。
- 零样本BFCL-v3评估（Table 4）。
- 缩放实验（Figure 2）：K值从1到16、数据从25%到100%的曲线。
- 刷新轮数影响（Figure 3）：R=0~5轮在三个模型上的表现。

**实验充分性评价**：实验设计系统全面，覆盖多benchmark、多模型规模、多基线对比，消融深入，鲁棒性及缩放分析丰富。结果公平，所有方法使用相同专家轨迹和评估协议。GRPO因Mind2Web限制未运行，但已说明理由。主观上实验充分且客观。

## 4. 资源与算力
- **明确说明**：Agentic-DPO在4块A6000 GPU上训练。
- 超参数：λ=0.5, β=0.008, K=4, R=5, 有效batch size=16。
- **训练成本对比**（第5节讨论）：在τ-bench上9B模型，以SFT每步时间为1x，Agentic-DPO为1.6x（因每次需前向专家和负例两次），GRPO则为13.6x（需完整多轮环境交互的rollout）。Agentic-DPO仅需学生做一步动作采样，无需环境交互。
- 未说明具体训练总时长（如小时数），但提供了相对成本比较。

## 5. 实验数量与充分性（总结与判断）
- **数量充分**：涵盖3个涉及不同类型动作空间与交互模式的数据集，使用了3个模型规模及一个额外模型做消融；对比基线包括在线RL（GRPO）、轨迹DPO（ETO）、奖励校正SFT（DFT）等强基线，加基础SFT和PPA+SFT共6个方法。
- **消融全面**：将4个关键组件分别移除，并在两个模型上验证，结果清晰显示每个组件的必要性。
- **鲁棒性测试**：9种扰动分类分析，区分在类/未见类，并以BFCL-v3做零样本泛化测试。
- **缩放分析**：K值和数据量两个维度均展示饱和趋势，结论合理。
- **主观评价**：实验设计严谨，结果客观，避免了当前常见的数据泄漏或评估偏差。唯一不足是R轮数在小模型上需调优，但论文提供了讨论并公开代码以确保可复现。

## 6. 论文的主要结论与发现

1. Agentic-DPO在所有测试benchmark和模型规模上一致显著超越SFT，平均增益达18~25个百分点。
2. Agentic-DPO在τ-bench零售上将9B模型从21.7%提升至41.4%，匹配在线GRPO（40.0%），但训练成本仅为后者的约1/6（1.6x vs 13.6x），且无需环境交互。
3. PPA是稳定训练的关键组件，尤其对扰动鲁棒性贡献显著（移除后扰动-标准准确率差距从~4分扩大到11-15分）。
4. SFT预热是必备前提，否则学生采样无法产生有效负例导致完全失败。
5. 负例采样K=2~4即可饱和，更多K增益有限；数据效率高，仅使用25%专家数据即达到PPA+SFT用100%数据的性能。
6. 较大的模型（9B）只需一轮负例刷新即可达到最优，而小模型（2B）需要多轮。

## 7. 优点

- **方法创新**：将DPO偏好学习从轨迹级压缩到步骤级，仅需一步动作采样，实现了低成本离线策略优化；PPA有效分离潜在决策与表面模式，属重要技术贡献。
- **低成本高效率**：无需在线环境、奖励模型或全轨迹探索，训练成本远低于在线RL。
- **设计稳定**：SFT锚点+PPA+长度缩放三者共同保障训练稳定性和泛化性。
- **实验全面**：覆盖多种agent场景、模型尺度、基线，消融与鲁棒性分析透彻，数据缩放和刷新轮数分析揭示关键规律。
- **开源可复现**：代码已公开在GitHub。

## 8. 不足与局限

- **离线方法固有局限**：Agentic-DPO只构造专家状态上的偏好，无法覆盖学生因自身长程行为而产生的新状态（与在线RL的区别），对于需大幅探索超出专家数据范围的任务仍受限。
- **依赖专家数据覆盖度**：若专家轨迹未覆盖重要决策状态，方法无法在这些状态上提供偏好信号，适用场景为专家数据充分的情况。
- **负例构造依赖当前学生**：小模型需多轮刷新才能获得有效负例，刷新鲜在更复杂场景下的收敛性未充分探讨。
- **未测试更大模型**：实验最大仅到9B，在更大模型（例如70B）上的表现和成本未知。
- **超参数固定**：β、λ、K、R等参数在所有实验固定，未对每个任务单独调优（尽管论文声明泛化性好，但可能存在任务间最优值差异）。
- **无理论保证全局收敛**：仅给出局部一阶分析（命题1），尚缺乏对全局最优性的理论支撑。

（完）
