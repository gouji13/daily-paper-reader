---
title: "Theoretical Foundations of $\\max$@$k$ Reinforcement Learning"
title_zh: "$\\max$@$k$强化学习的理论基础"
authors: "Riccardo Poiani, Martino Bernasconi, Andrea Celli"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17823v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 大模型强化学习的理论基础
tldr: "max@k评估在代码生成等强化学习任务中广泛应用，但理论基础薄弱。本研究证明优化max@k目标与标准期望回报最大化本质不同，马尔可夫策略一般不足，通过状态增广可恢复最优性，并刻画了历史依赖与非历史依赖策略的性能差距。进一步表明学习max@k最优策略统计难度更高，所提算法达到最优样本复杂度。该工作为max@k强化学习提供了理论奠基，推动了实用强化学习的理论理解。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17823v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 583, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17823v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 420, \"height\": 282, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17823v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 1058, \"label\": \"Figure\"}]"
motivation: "max@k评估在代码生成等任务中重要但缺乏理论理解，需分析其最优策略性质和学习难度。"
method: "通过理论分析对比max@k与标准RL，引入状态增广恢复马尔可夫性，设计高效算法。"
result: 证明马尔可夫策略不足，刻画性能差距，给出最优样本复杂度的算法。
conclusion: "为max@k强化学习奠定理论基础，揭示其统计难度，指导实际应用。"
---

## 摘要
强化学习是现代大型推理模型的关键技术。通常，对于代码生成和定理证明等困难任务，智能体通过生成K个响应而非单个响应来进行评估，然后使用诸如max@k这样的重试感知指标来衡量性能。尽管这些指标具有实际重要性，但在此类标准下学习的理论基础仍然有限。在本工作中，我们对有限时域强化学习中的max@k学习问题进行了理论研究。我们表明，优化max@k目标与标准期望回报最大化有本质区别。特别地，我们证明了马尔可夫策略通常是不充分的，识别了一种恢复最优性的紧凑状态增强方法，并明确刻画了历史依赖策略与非历史依赖策略之间可能出现的性能差距。此外，我们表明学习最优的max@k策略在统计上比标准强化学习更困难，并提供了一个达到最优样本复杂度率的有效算法。

## Abstract
Reinforcement Learning is a cornerstone technique for modern large reasoning models. Usually, for difficult tasks such as code generation and theorem proving, the agent is evaluated by generating $K$ responses rather than sampling a single response, and performance is then measured using a retry-aware metric such as $\max$@$k$. Despite their practical importance, the theoretical foundations of learning under such criteria remain limited. In this work, we provide a theoretical study of the $\max$@$k$ learning problem in finite-horizon reinforcement learning. We show that optimizing the $\max$@$k$ objectives is fundamentally different from standard expected-return maximization. In particular, we prove that Markovian policies are in general insufficient, identify a compact state augmentation that restores optimality, and explicitly characterize the performance gap that can arise between history-dependent and non-history-dependent policies. Moreover, we show that learning $\max$@$k$-optimal policies is statistically harder than standard reinforcement learning and provide an efficient algorithm that achieves the optimal sample complexity rate.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

论文关注的是强化学习（RL）中的一个重要实践问题：在代码生成、定理证明等复杂推理任务中，智能体通常被允许生成多个候选答案（K个响应），并基于其中的最佳结果进行评估，即采用 **max@k** 指标（或 pass@k 的泛化形式）。这种评判方式与标准RL中期望累积回报最大化存在本质差异，因为 max@k 对轨迹分布是非线性的。

尽管 max@k 在大型语言模型（LLM）等现代推理系统中广泛应用，但其背后的理论基础非常薄弱。现有RL理论大多依赖于期望累积回报的线性可加性，而 max@k 破除了这一假设。因此，论文旨在系统性地建立 max@k 强化学习的理论基础，明确其与经典RL在最优策略形式、计算复杂度和样本复杂度上的根本区别。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：该文通过理论分析揭示 max@k RL 在规划与学习中的独特挑战，并设计高效算法。

**关键技术细节**：

- **策略空间分析**：
  - 证明马尔可夫策略（仅依赖当前状态、步骤和 rollout 索引）对于 max@k 目标不是最优的，例子表明历史依赖策略可取得显著更好的性能。
  - 但完全的历史依赖并非必要：证明可通过压缩历史信息——仅保留（当前状态、之前 rollouts 中获得的最佳奖励 M、当前 rollout 中已累积的奖励 G）来恢复最优性，从而定义策略类 Π^C。

- **规划问题**：
  - 提出基于离散化的近似规划算法：将奖励值均匀离散化到 κ 精度的网格，然后在离散化 MDP M_κ 上用动态规划求解最优策略。该算法可在 O(KS^2 A H^3/κ^2) 时间内计算，通过设置 κ = ε/H 获得 ε-最优策略（多项式时间近似方案）。
  - 同时证明精确规划问题是 NP-hard 的（归约于子集和问题），因此近似方案是必要的且已经最优。

- **学习问题**：
  - 采用生成模型（generative model）进行 PAC 学习。算法分两步：首先均匀采样每个状态-动作对 m 次构建经验转移核，然后将奖励离散化，通过动态规划求解经验 MDP 上的最优策略。
  - 理论分析给出下界 Ω(K S A H^3 ε^{-2} log(1/δ))，上界为 Õ(K S A H^3 ε^{-2} log(1/δ))（时间齐次情形），表明 max@k 学习的样本复杂度是经典RL的 K 倍（K 为 rollout 数量）。

**公式/算法流程**（文字说明）：
1. 初始化采样次数 m。
2. 对每个 (s,a,h) 采样 m 次观测转移 s'。
3. 构造经验转移核 {p̂_h}。
4. 将奖励投影到离散网格 R_κ。
5. 在经验 MDP 上用动态规划（基于压缩状态 (s, M, G)）计算最优策略 μ̂。
6. 返回 μ̂。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

**论文是纯理论性质，没有进行模拟或真实实验**。所有结果都是通过数学证明获得的。因此没有使用任何数据集、benchmark 或对比方法。论文中的“示例”仅用于说明理论观点（如马尔可夫策略非最优的构造、信息压缩的必要性等），并非实验性验证。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

**论文未提及任何计算资源或算力消耗**。作为理论工作，没有进行实际训练或推理，因此不存在 GPU 使用情况。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

**没有传统意义上的实验**。论文的主要“实验”是基于数学推导和构造性反例的理论分析。例如：
- 用一个简单MDP例子（Figure 2）说明马尔可夫策略的次优性。
- 用另一个例子（Figure 3）证明丢失 M 或 G 任一信息都会导致性能损失。
- 在统计下界中构造了一个两动作伯努利奖励的实例。
- 在计算复杂度下界中使用了子集和问题的归约。

这些理论证明是充分和严格的，但在工程可验证性方面，缺少数值模拟或实际任务上的指标。对于理论论文而言，这样的分析是充分的；但若要评估方法在实际 LLM 等系统上的效果，还需后续实证研究。

### 6. 论文的主要结论与发现

- **马尔可夫策略不足**：max@k 目标下，马尔可夫策略可能远劣于历史依赖策略，且存在乘法常数大于1的差距。
- **历史可压缩**：最优策略可压缩为只依赖当前状态、之前最佳回报和当前累计回报的三元组，这定义了实用的策略类 Π^C。
- **规划复杂度**：存在多项式时间近似方案（FPTAS）可计算 ε-最优策略，但精确规划是 NP-hard 的，因此近似是必要的。
- **样本复杂度**：max@k 学习的样本复杂度下界为 Ω(K S A H^3 ε^{-2} log(1/δ))，算法达到匹配的上界，说明统计难度是经典RL的 K 倍。
- **对齐实际启示**：模型说明了为什么自适应推理方法（如多次尝试、验证反馈）能在实践中提升性能，因为它们本质上实现了历史依赖策略。压缩结果也解释了实践中只保留关键信息（如最佳结果）的设计原理。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论贡献全面**：同时涵盖了最优策略结构、规划复杂度和学习复杂度三个层面，为 max@k RL 建立了系统性的理论框架。
- **结果严格且紧**：下界与上界匹配（仅对数因子差异），证明了 K 因子是本质的。
- **实际相关性**：理论推导与当前 LLM 模型部署中的重试策略、推理缩放等现象紧密联系，具有很强现实指导意义。
- **方法简洁有效**：压缩表示和离散化技术使得理论分析可操作，算法计算高效（多项式时间），无复杂结构。
- **证明技巧创新**：对 max@k 非线性的处理（如通过构造实例分离 M 和 G 的必要性、归约于子集和、方差控制等）展现了高水平的数学功底。

### 8. 不足与局限

- **缺乏实证验证**：所有结果停留在理论层面，没有提供在模拟环境或真实LLM上的实验支持，因此实际有效性和算法实现问题尚未检验。
- **生成模型假设**：学习部分假设有生成模型，即可以查询任意状态-动作对的转移样本。这与实际在线强化学习（如 RLHF 中的前向交互）有距离，推广到在线设置需要更多工作。
- **时间齐次性假设简化**：在样本复杂度上界中，主要针对时间齐次 MDP 给出紧界，时间非齐次情形多了 H 因子，可能不是最优。
- **忽略连续状态/动作空间**：论文假设有限状态和动作，未讨论连续情况，而 LLM 的状态空间通常是高维且连续的。
- **计算复杂度分析仅针对规划**：学习算法的计算复杂度（动态规划部分）已给出，但未分析采样阶段的计算瓶颈（虽然采样是单纯的 I/O 操作）。
- **可扩展性问题**：压缩后的状态空间大小为 S × O(H/κ) × O(H/κ)，当 H 很大或 κ 很小时仍然可能较大，但理论上是多项式，实际实现可能需进一步优化。

（完）
