---
title: "WAR: Workload-Aware Rollouts for Synchronous Agentic Reinforcement Learning"
title_zh: WAR：面向同步智能体强化学习的工作负载感知轨迹生成
authors: "Ryan Xu, Atlas Zhao, David Bao, Frank Du"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17299v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 同步Agentic RL的负载感知规划
tldr: 长程rollout生成成为同步agentic RL的主要系统瓶颈，WAR通过联合优化解码与调度来加速。低负载时采用SuffixDecoding重用已完成轨迹的后缀模式进行模型无关投机解码，避免额外模型干扰；高负载时切换至缓存感知调度，基于缓存局部性和负载均衡放置请求，减少KV-cache重计算。实验表明，WAR在低负载下提升1.4倍吞吐量，高负载下提升1.6倍，有效消除rollout瓶颈，为大规模长上下文智能体训练提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17299v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 752, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 879, \"height\": 828, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 794, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 872, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 870, \"height\": 824, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 867, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 847, \"height\": 1294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 864, \"height\": 582, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17299v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 865, \"height\": 571, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17299v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17299v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 656, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17299v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 399, \"height\": 244, \"label\": \"Table\"}]"
motivation: 长程rollout生成导致同步agentic RL训练受限于系统瓶颈，现有方法在高低负载下效率不足。
method: 低负载用SuffixDecoding投机解码重用后缀；高负载用缓存感知调度优化请求放置和负载均衡。
result: 低负载提升1.4倍吞吐量，高负载提升1.6倍吞吐量，减少冗余计算和负载不均。
conclusion: WAR消除同步agentic RL的主要rollout瓶颈，实现可扩展的长上下文智能体训练。
---

## 摘要
长程轨迹生成已成为智能体强化学习（RL）中的主要系统瓶颈。当智能体与环境进行多轮交互时，轨迹迅速增长到数万个token，这使得同步RL训练越来越受到轨迹生成的限制。我们提出WAR，一种工作负载感知的轨迹生成系统，通过联合优化解码和调度来显著加速同步智能体RL。WAR基于一个关键观察：最优轨迹生成优化策略取决于运行时负载：（1）在低负载下，WAR通过SuffixDecoding实现无模型的推测解码，该技术利用先前完成轨迹中的后缀模式作为未来轨迹生成的推测草稿。与基于模型的草稿器不同，SuffixDecoding不引入额外的草稿模型，并避免与轨迹生成产生GPU争用。（2）在高负载下，当饱和的批量解码留给推测加速的空间有限时，WAR将优化重点转向缓存感知调度。全局调度器根据缓存局部性、轨迹进度和服务器负载在轨迹生成副本间分配请求，从而减少冗余的KV缓存重计算并缓解负载不均衡。通过将解码层的后缀复用与系统层的轨迹生成调度相结合，WAR在工作负载场景下提供稳健的吞吐量提升，且无需改变底层RL算法。在低负载下，WAR将长上下文智能体轨迹生成吞吐量提升1.4倍，高负载下提升高达1.6倍。这些结果表明，WAR消除了同步智能体RL中的一个主要轨迹生成瓶颈，并为可扩展的长上下文智能体训练提供了一条实用路径。

## Abstract
Long-horizon rollout generation has become the dominant systems bottleneck in agentic reinforcement learning (RL). As agents interact with environments over many turns, trajectories rapidly grow to tens of thousands of tokens, making synchronous RL training increasingly constrained by rollout. We propose WAR, a workload-aware rollout system that substantially accelerates synchronous agentic RL by jointly optimizing decoding and scheduling. WAR is built on a key observation: the optimal rollout optimization strategy depends on runtime load: (1) Under low load, WAR enables model-free speculative decoding with SuffixDecoding, which reuses suffix patterns from previously completed trajectories as speculative drafts for future rollouts. Unlike model-based drafters, SuffixDecoding introduces no additional draft model and avoids GPU contention with rollout generation. (2) Under high load, where saturated batched decoding leaves limited room for speculative speedup, WAR shifts the optimization focus to cache-aware scheduling. A global scheduler places requests across rollout replicas based on cache locality, trajectory progress and server load, reducing redundant KV-cache recomputation and mitigating load imbalance. By combining decoding-level suffix reuse with system-level rollout scheduling, WAR delivers robust throughput improvements across workload regimes without changing the underlying RL algorithm. WAR improves long-context agentic rollout throughput by 1.4x under low load and up to 1.6x under high load. These results show that WAR removes a major rollout bottleneck in synchronous agentic RL and provides a practical path toward scalable long-context agent training.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在同步智能体强化学习（Synchronous Agentic RL）中，长程（long-horizon）轨迹生成（rollout generation）已成为主导性的系统瓶颈。智能体与环境多轮交互导致轨迹上下文迅速增长至数万token，同步训练中所有生成任务必须完成后才能进入下一阶段，而长尾轨迹（少数极长轨迹）导致大量GPU空闲，严重降低资源利用率。
- **整体含义**：现有优化方法（如推测解码、简单调度）在不同负载下效果差异显著，缺乏自适应能力。论文主张将运行时工作负载（workload）视为一阶因素，提出WAR系统，通过联合优化解码和调度来适配不同负载，实现稳健的吞吐量提升，从而消除同步rollout瓶颈，为大规模长上下文智能体RL训练提供实用路径。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：根据运行时工作负载自适应切换优化策略。
  - **低负载**（GPU未饱和）：开启模型无关的推测解码——**SuffixDecoding**，利用已完成轨迹中的后缀模式作为草稿，目标模型并行验证，无需额外草稿模型，避免GPU争用。
  - **高负载**（GPU已饱和）：关闭推测解码（避免额外开销），切换至**缓存感知调度**，减少冗余KV-cache重计算和负载不均衡。
- **关键技术细节**：
  - **SuffixDecoding**：基于后缀树缓存历史轨迹中的token序列模式，在解码时匹配当前上下文生成多个候选token，并行验证。支持跨worker和后缀树同步（每轮RL结束后广播新轨迹）。
  - **缓存感知调度**：
    - 全局调度器对排队请求进行评分，考虑四个因素：
      - `S_waiting`：请求在队列中的等待时间（优先级最高，确保不超沙盒TTL终止）。
      - `S_turn`：在线估计的助手轮数（`ˆT_g`，通过指数平滑更新），负分偏向短轨迹以提前释放资源。
      - `S_cache`：缓存命中率H（从服务器直接获取），正分鼓励路由至已有缓存的副本。
      - `S_inflight`：每个副本的正在处理请求数，负分用于负载均衡。
    - 评分线性组合，系数顺序：`|S_wait| ≫ |S_turn| ≫ |S_cache| ≫ |S_inflight|`。
    - 采用基于AIMD的窗口控制（借鉴CONCUR），根据平均缓存命中率动态调整每个副本的最大并发请求数。
  - **工作负载指标**：使用运行时有效批量大小（effective batch size）作为轻量级指示器，通过经验确定切换阈值。

## 3. 实验设计：数据集、基准、对比方法
- **数据集与场景**：使用**SWE（Software Engineering）数据集**，包含500个prompts，模拟多轮代码修复任务（智能体需查看文件、执行命令、迭代调试）。
- **基准**：基于**veRL v0.8.0**（同步RL框架）作为基线，分别比较以下四种配置：
  - `veRL base`：无任何优化（默认least-request + sticky session）
  - `sched only`：仅启用缓存感知调度
  - `spec only`：仅启用SuffixDecoding
  - `sched spec`：同时启用两者
- **对比指标**：端到端rollout时间、预填充吞吐量（prefill throughput）、解码吞吐量（decode throughput）、KV-cache命中率、SuffixDecoding接受长度（acceptance length）。
- **消融实验**：
  - 关闭等待因子（`S_waiting`）对比完整调度。
  - 单独评估各调度因子：仅`S_turn`、仅`S_cache`、仅`S_inflight` vs 完整`sched only`。
  - 分析SuffixDecoding在不同batch size下的收益对比（固定输入输出长度测试）。
  - 探究接受长度的来源（跨prompt相似性 vs 同prompt内多次rollout）。

## 4. 资源与算力
- 明确说明：使用**InfiniBand连接的H100集群**，共**2个节点，每节点8张GPU**，总计**16张H100 GPU**。
- 模型：**Qwen3-32B**，通过YaRN扩展至96K上下文长度。
- 并行设置：
  - 训练侧：TP=4, PP=2, CP=2
  - Rollout侧：TP=4
- 训练批大小从4到64变化（具体取4,8,16,32,64），每个prompt rollout 8次（`rollout_n=8`），每个配置运行**5个训练步骤**。未报告单次训练总时长，但提供了吞吐量对比。

## 5. 实验数量与充分性
- **实验数量**：主要端到端实验覆盖5个批大小，每个配置重复次数未明确，但图中显示有error bar（标准偏差），至少多个随机种子。消融实验包括约3~4组，加SuffixDecoding单独测试、接受长度来源分析等，总计约10余组实验。
- **充分性**：
  - **积极方面**：控制了生成工作量的差异（确保不同配置的prefill tokens、decode tokens、assistant turns差异在5%以内），对比公平；工作负载从低到高完整覆盖；消融实验验证了各组件贡献。
  - **局限性**：
    - 仅使用了**单一模型（Qwen3-32B）**和**单一任务（SWE）**，未在不同模型（如Llama系列）或其他类别的agent任务（如数学、代码生成）上验证，泛化性存疑。
    - 从图中看，每个点仅5步，可能不足以捕捉训练过程中的动态变化（如模型更新后SuffixDecoding效果变化）。
    - 未与异步rollout方法（如DAS、AResL）或其他调度方法（如RollPacker、Seer）直接比较，仅与veRL基础策略对比。

## 6. 论文的主要结论与发现
- **主要结果**：
  - `sched spec`（两者都开）在所有批大小下均优于独立配置：低负载（批大小4）预填充吞吐量提升35%、解码吞吐量提升32%，端到端时间降低约1.4×；高负载（批大小64）预填充和解码吞吐量均提升63%，端到端时间降低约1.6×。
  - `sched only`在高负载下效果显著（预填充提升0%~47%，解码提升-4%~47%），低负载时几乎无收益甚至轻微负收益。
  - `spec only`在低负载下表现突出（预填充提升53%~5%，解码提升48%~5%），高负载下收益消失。
  - **互补效应**：缓存感知调度在高负载下通过维持高缓存命中率（从0.1提升至0.5）减少重计算；SuffixDecoding在低负载下通过利用重复模式实现高接受长度（约2.8~6.0），增加GPU并行度。
- **其他发现**：
  - 等待因子对于高负载下防止沙盒超时至关重要（关闭时高负载下rollout严重退化）。
  - 缓存因子（`S_cache`）是调度中最有效的单一因素。
  - 接受长度来源同时来自跨prompt相似性和同prompt多次rollout的局部重复。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：
  - **首次将工作负载作为一阶因素**引入rollout优化，打破“一刀切”策略，自适应切换解码加速与系统调度。
  - **模型无关的SuffixDecoding**：无额外草稿模型，无GPU争用，天然适配RL rollout中因多轮交互产生的重复模式，同时保持生成分布不变。
  - **缓存感知调度**：综合考虑缓存局部性、轨迹长度估计、负载均衡和等待时间，且使用AIMD窗口控制稳定运行时，减少冗余KV-cache重计算。
  - **跨worker后缀树同步**：每轮RL后广播新轨迹，使各副本共享模式，提升草稿覆盖率。
- **实验设计亮点**：
  - **严格的工作量控制**：过滤差异超过5%的回合，确保吞吐量对比反映系统优化而非随机生成差异。
  - **解耦分析**：分别评估解码和调度组件，并展示其互补效应，验证自适应必要性。
  - **消融实验全面**：逐一分析调度公式中每个因子的贡献，并单独测试SuffixDecoding在不同批大小下的收益，逻辑清晰。

## 8. 不足与局限
- **实验覆盖不足**：
  - 仅使用**一个模型（Qwen3-32B）**和一个任务类型（SWE agent），未在其他规模模型（如7B、70B）或不同领域任务（如数学推理、代码生成）上验证，泛化能力不确定。
  - 仅运行5个训练步骤，未评估长期训练中模型更新对SuffixDecoding匹配效果的影响（草稿可能随模型行为变化而失效）。
- **未与先进方法对比**：
  - 未与异步rollout系统（如AReaL、StreamRL）或基于内容的调度（如RollPacker的tail batching、Seer的上下文感知调度）直接比较，缺乏相对优势的定量证据。
  - 未与基于模型的推测解码（如Medusa、Eagle）在同等计算资源下对比，难判断模型无关方法的实际优越性。
- **实现依赖经验调参**：
  - 调度评分系数（`scale_wait`, `scale_turn`等）和SuffixDecoding启用阈值基于经验确定，未提供自动调优机制，可能在不同硬件或负载下需重新调整。
- **理论上未考虑分布式训练场景**：
  - 论文假设全局调度器存在，但若rollout副本分布在多个节点且网络延迟较高，调度开销可能抵消收益（虽已提及避免过调度，但未量化分析）。
- **写作者偏差风险**：
  - 作者来自同一团队，且所有实验均在自家修改的veRL上执行，缺少第三方独立复现验证。

（完）
