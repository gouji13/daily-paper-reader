---
title: Progress- and Reliability-Oriented Group Policy Optimization for Agentic Reinforcement Learning
title_zh: 面向进展与可靠性的群体策略优化在智能体强化学习中的应用
authors: "Mingxuan Fan, Peiyang Liu"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04242v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向智能体强化学习的组策略优化
tldr: 基于组的强化学习在步级优化中面临分组公平性与覆盖范围的权衡。ProGPO提出无需学习评论家的上下文一致步级学习方法，通过精确前缀动作比较和基于rollout状态势能的过渡信用分配，并结合语义扩展与逆方差融合来可靠估计势能。在ALFWorld和WebShop任务上，ProGPO以相近计算开销取得优于基线的性能，并在Qwen2.5-3B上验证了可扩展性，为长时交互agent策略学习提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04242v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 360, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1421, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1425, \"height\": 791, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1450, \"height\": 451, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1435, \"height\": 530, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04242v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1460, \"height\": 733, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04242v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04242v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 298, \"label\": \"Table\"}]"
motivation: 现有步级组策略优化中，分组方式难以兼顾覆盖范围与公平比较，导致策略更新不稳定且信号稀疏。
method: ProGPO保持精确前缀动作比较，利用基于rollout状态势能的过渡信用补偿同伴信号，并采用语义扩展与逆方差融合估计势能。
result: 在ALFWorld和WebShop上，ProGPO超越匹配的agentic RL基线，且Qwen2.5-3B实验验证了方法可扩展性。
conclusion: ProGPO是一种无需学习评论家的有效步级分组优化方法，提升了长时交互agent策略学习的稳定性与效率。
---

## 摘要
基于群体的强化学习已成为改善大型语言模型在长周期交互任务中表现的有效范式。为获得比轨迹级优化更细粒度的策略更新，近期研究转向步骤级群体强化学习，其中中间步骤被分组并在展开批次内进行比较。然而，步骤级优势估计对分组方式敏感：按宽泛状态键分组可提升覆盖率，但可能比较不同历史条件下的动作；而强制历史一致性会产生更公平的比较，但代价是群体碎片化及缺失同伴比较信号。本文提出ProGPO（面向进展与可靠性的群体策略优化），一种无需学习评判器的上下文一致步骤级学习方法。ProGPO保留精确前缀动作比较，并通过基于展开状态势的转换信用补充稀疏的同伴比较。为可靠估计这些势，ProGPO结合语义扩展与跨历史深度的逆方差融合。我们在ALFWorld和WebShop两个具有挑战性的智能体任务上，使用Qwen2.5-1.5B-Instruct进行评估。结果表明，在可比计算开销下，ProGPO优于匹配的智能体强化学习基线，额外的Qwen2.5-3B-Instruct实验进一步检验了所提方法的可扩展性。

## Abstract
Group-based reinforcement learning (RL) has become an effective paradigm for improving large language model agents on long-horizon interactive tasks. To obtain finer-grained policy updates than trajectory-level optimization, recent work has moved toward step-level group-based RL, where intermediate steps are grouped and compared within a rollout batch. However, step-level advantage estimation is sensitive to how groups are formed: grouping by broad state keys improves coverage but may compare actions taken under different histories, while enforcing historical consistency yields fairer comparisons at the cost of fragmented groups and missing peer-comparison signal. In this paper, we propose ProGPO (Progress- and Reliability-Oriented Group Policy Optimization), a learned-critic-free method for context-consistent step-level learning. ProGPO keeps exact-prefix action comparison, and complements sparse peer comparisons with transition credit derived from rollout-based state potentials. To estimate these potentials reliably, ProGPO combines semantic expansion with inverse-variance fusion across history depths. We evaluate ProGPO on two challenging agentic tasks, ALFWorld and WebShop, with Qwen2.5-1.5B-Instruct. Results show that ProGPO improves over matched agentic RL baselines under comparable computational overhead, and additional Qwen2.5-3B-Instruct experiments further test the scalability of the proposed method.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLM）作为智能体在长周期交互任务（如网页导航、具身家务）中面临稀疏延迟奖励下的信用分配难题。基于群体的强化学习（如GRPO）通过多轨迹比较估计优势，避免学习评论家，但在步骤级优化中，分组方式面临公平性与覆盖范围的权衡：按宽泛状态键分组可增加样本覆盖，但可能比较不同历史条件下的动作，产生偏置；强制历史一致性（精确前缀匹配）能实现公平比较，但导致群体碎片化，大量步骤成为孤立点或低对比组，无法获得同伴比较信号。
- **整体含义**：ProGPO旨在解决上述权衡，在不引入额外rollout或学习评论家的情况下，通过从rollout批次统计中估计状态势能，为上下文一致的步骤级学习补充进展信用（transition credit），从而在保持动作比较公平性的同时，提供更密集的学习信号。

## 2. 方法论

### 核心思想
- **分离动作比较与进展估计**：动作相对优势通过精确前缀组获得（保持公平性），而状态进展信用通过从rollout批次中估计的非参数状态势能推导。
- **多分辨率融合与语义扩展**：状态势能估计利用不同历史深度的前缀组，通过逆方差加权融合（依据样本量和返回方差）提高可靠性；当精确组样本不足时，通过语义相似性（策略隐藏状态嵌入）扩展查找邻居，稳定势能估计。

### 关键技术细节
1. **前缀一致步骤比较**：  
   - 定义深度为k的精确观察前缀组 \( G^{(k)}(p_t^{(k)}) \)，计算组相对优势 \( A_t^{(k)} \)。  
   - 通过所有有效深度的加权聚合得到层次优势 \( A_t^{\text{hier}} \)，权重为 \( (k+1)^{\alpha_h} \)，鼓励更深的上下文匹配。

2. **状态转换信用**：  
   - 对于每个深度k，估计状态势能 \( \hat{V}^{(k)}(p_t^{(k)}) \) 为组内平均折扣返回。  
   - **可靠性感知多分辨率融合**：从所有满足最小样本阈值的深度中，按逆方差权重 \( w_k = n_k / (\sigma_k^2 + \epsilon_{\text{var}}) \) 融合得到 \( \hat{V}^{\text{mr}}(s_t) \)。  
   - 过渡信用 \( A_t^{\text{trans}} = \hat{V}^{\text{mr}}(s_{t+1}) - \hat{V}^{\text{mr}}(s_t) \)。

3. **语义相似性扩展**：  
   - 当 \( k=1 \) 的精确组样本量小于 \( m \) 时，检索最相似的 \( K_{\text{sem}} \) 个邻居（基于策略隐藏状态嵌入），用相似度加权平均扩展势能估计。

4. **互补信用组合**：  
   - 定义指示符 \( c_t \) 表示是否存在有效同伴比较，残差门控 \( \eta_t = 1 - (1-\lambda_{\text{fb}})c_t \)。  
   - 最终优势 \( A_t^{\text{ProGPO}} = A_t^{\text{hier}} + \lambda_{\text{tr}} \eta_t A_t^{\text{trans}} \)。

5. **策略优化目标**：标准截断替代目标，附加KL惩罚。

## 3. 实验设计

### 数据集/场景
- **ALFWorld**：具身家务任务（找物品、加热、清洁等），最多50步，稀疏成功/失败奖励。
- **WebShop**：网页购物任务，按自然语言指令搜索和购买商品，最多30步，稀疏奖励。

### Benchmark
- 标准长周期智能体基准，评估成功率和任务分数。

### 对比方法
- **Prompt-based baselines**：GPT-4o、Gemini-2.5-Pro、Qwen2.5、ReAct、Reflexion（引自先前报告）。
- **RL-based baselines**：PPO、RLOO、GRPO、GiGPO、HGPO（GRPO、GiGPO、HGPO为本地重跑，PPO、RLOO引用之前报告）。
- **ProGPO**：本文方法。

### 模型规模
- 主要实验：Qwen2.5-1.5B-Instruct（匹配验证）。
- 扩展性测试：Qwen2.5-3B-Instruct（仅ALFWorld训练/验证/测试分数比较）。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量及训练时长。仅提及训练细节：batch size=16，rollout group size=8，学习率1e-6，KL系数0.01，使用vLLM进行rollout生成。ProGPO的计算开销与基线相当，无需额外rollout或参数评论家。

## 5. 实验数量与充分性

- **主要对比实验**：在ALFWorld和WebShop上，对GRPO、GiGPO、HGPO、ProGPO进行匹配验证（3个种子，训练步数160），报告平均值与标准差（表1）。此外，包含大量prompt-based和RL-based参考结果。
- **消融实验**：在ALFWorld训练rollout上进行了过渡信用成分消融（表3），对比“仅过渡信用”、“+语义扩展”、“+可靠性融合”、“+两者”四种配置，验证每个组件的贡献。
- **机制诊断**：分析步骤信号覆盖情况（图5、表2），包括peer-zero比例、transition-active比例、多分辨率融合使用频率等。
- **扩展性实验**：Qwen2.5-3B上比较GiGPO、HGPO、ProGPO在ALFWorld上的训练/验证成功率和测试分数（步155）。
- **充分性评价**：实验覆盖两个不同环境（具身+网页）、两个模型规模、多个基线、消融与诊断，比较全面。但仅在两个环境上验证，且模型规模最高3B，未涉及更大模型或多模态任务。消融实验仅针对ALFWorld训练侧，缺乏WebShop消融。整体客观、公平，但范围有限。

## 6. 主要结论与发现

- ProGPO在ALFWorld整体成功率上达到90.1%，优于GRPO（75.8%）、GiGPO（85.5%）、HGPO（87.8%）；在WebShop上成功率71.5%，优于GiGPO（67.6%）、HGPO（65.6%）。
- 过渡信用有效补充了同伴比较信号：11.4%的步骤仅通过过渡信用获得信号，43.0%-63.3%的步骤至少激活一种信号。
- 多分辨率融合和语义扩展对过渡信用估计至关重要：单独移除任一组件均导致性能下降，两者结合效果最佳（ALFWorld训练成功率从87.5%提升至96.9%）。
- 在3B模型上，ProGPO保持优势：训练/验证成功率及测试分数均高于GiGPO和HGPO。

## 7. 优点

- **无需学习评论家**：所有势能估计均来自rollout批次统计，无需额外训练价值网络或奖励模型。
- **保持动作比较公平性**：通过精确前缀分组，确保只有相同历史条件下的动作被直接比较。
- **密集信用分配**：使用过渡信用为孤立步骤提供进展信号，弥补同伴比较的稀疏性。
- **可靠性设计**：多分辨率融合（逆方差加权）和语义扩展使势能估计在少样本场景下更稳定。
- **计算开销低**：仅利用现有rollout隐藏状态，无额外rollout或模型前向传播。

## 8. 不足与局限

- **残留零信号步骤**：即使应用过渡信用，仍有部分步骤（WebShop 22.3%、ALFWorld 33.1%）未获得任何有效优势，因为当前/下一状态势能相同或缺乏可靠证据。
- **非参数势能依赖状态表示质量**：语义扩展使用策略隐藏状态，不能保证语义相近的状态具有相似期望回报；检索噪声可能引入偏差。
- **环境限制**：仅在ALFWorld和WebShop上验证，未覆盖更开放的网页任务或多模态环境。模型规模限于3B，更大模型的行为可能不同。
- **消融范围有限**：仅针对过渡信用成分做消融，缺少对多分辨率融合参数、语义扩展邻居数、超参数（如 \(K_h, m, \lambda_{\text{tr}}\) 等）的系统性扫描。
- **过渡信用非因果**：\( \hat{V}(s_{t+1}) - \hat{V}(s_t) \) 可能受环境随机性、任务难度或表示误差影响，不一定反映动作的因果效应。
- **算力信息未公开**：无法评估训练成本或进行复现比较。

（完）
